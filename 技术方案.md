# 财务管理系统 — 技术方案

> 版本：第一稿（v1.0）
> 编制日期：2026-08-29
> 交付形态：Windows 桌面单文件可执行程序（`财务管理系统.exe`，约 120 MB）

---

## 1. 项目概述

### 1.1 产品定位

本系统是一款面向中小企业财务人员的**桌面端财务管理软件**，核心特色在于将 **OCR 票据智能识别** 与 **账务管理** 深度结合：用户可通过扫描或导入发票、凭证、银行回单等票据影像/PDF，系统自动识别票面要素并生成记账凭证，减少手工录入工作量。

### 1.2 目标用户与使用场景

| 用户角色 | 典型场景 |
|---|---|
| 出纳/会计 | 发票录入、凭证填制、日记账登记、银行对账 |
| 财务主管 | 科目余额查询、财务报表生成、结账审核 |
| 小微企业主 | 收支流水查看、应收应付跟踪、简易报税数据准备 |

### 1.3 运行环境

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows 10 / 11（64 位） |
| 运行时 | 免安装，PyInstaller 已内嵌 Python 3.13 运行时 |
| 磁盘空间 | 安装包约 120 MB，运行时解压至 `_internal` 约 150 MB |
| 内存 | 最低 4 GB，推荐 8 GB（OCR 推理峰值约 1–2 GB） |
| 网络 | 本地运行，无需联网；OCR 模型内置，离线可用 |

---

## 2. 总体技术架构

### 2.1 架构分层

```
┌─────────────────────────────────────────────────────┐
│                   表现层 (Tkinter GUI)                │
│   主窗口 / 菜单 / 树形导航 / 数据表格 / 对话框         │
├─────────────────────────────────────────────────────┤
│                   业务逻辑层                            │
│  凭证管理 │ 账簿管理 │ 报表生成 │ 票据识别 │ 系统设置  │
├─────────────────────────────────────────────────────┤
│                   核心能力层                            │
│  OCR 引擎 │ 图像处理 │ PDF 解析 │ 版面分析 │ 财务计算  │
├─────────────────────────────────────────────────────┤
│                   数据访问层                            │
│          SQLite 本地数据库 (sqlite3 + _decimal)       │
├─────────────────────────────────────────────────────┤
│                   基础设施层                            │
│  Python 3.13 │ PyInstaller 打包 │ multiprocessing    │
└─────────────────────────────────────────────────────┘
```

### 2.2 技术选型总览

| 层级 | 技术组件 | 版本 | 用途 |
|---|---|---|---|
| 语言 | Python | 3.13 | 主开发语言 |
| 打包 | PyInstaller | — | onefile 单文件打包 |
| GUI | Tkinter / ttk | 8.6 | 桌面界面 |
| 数据库 | SQLite | 3 | 本地数据持久化 |
| OCR | RapidOCR (ONNX Runtime) | 1.2.3 | 中文票据文字检测与识别 |
| 推理引擎 | ONNX Runtime | — | CPU 端模型推理 |
| 图像处理 | OpenCV | — | 图像预处理、矫正、二值化 |
| 图像处理 | Pillow (PIL) | — | 图像格式转换、缩放 |
| 数值计算 | NumPy | 2.5.2 | 矩阵运算、数组处理 |
| PDF 处理 | PyMuPDF | — | PDF 页面渲染为图像、文本提取 |
| 几何计算 | Shapely | — | 版面区域几何运算 |
| 多边形裁剪 | pyclipper | — | 票据表格区域裁剪 |
| XML/HTML | lxml | — | 结构化文档解析 |
| 配置 | PyYAML | — | OCR 模型配置文件 |
| 高精度计算 | decimal (_decimal) | — | 金额精确计算，避免浮点误差 |
| 系统集成 | pywin32 / WMI | — | Windows 系统信息获取 |
| 并发 | multiprocessing | — | OCR 任务并行处理 |

### 2.3 选型理由

- **Tkinter**：Python 标准库内置，零额外依赖，打包体积小，适合轻量桌面工具；ttk 主题可满足基本美观需求。
- **SQLite**：单文件数据库，零配置，无需数据库服务器，适合单机桌面应用；支持事务，保证账务数据一致性。
- **RapidOCR + ONNX Runtime**：基于 PaddleOCR 的轻量化 ONNX 部署方案，CPU 即可运行，无需 GPU；内置中文检测/识别/方向分类三模型，识别精度满足票据场景；完全离线，数据不出本机，符合财务数据安全要求。
- **OpenCV + PyMuPDF**：覆盖图像票据和 PDF 票据两种输入形态；OpenCV 负责图像预处理（去噪、倾斜矫正、二值化），PyMuPDF 负责 PDF 高分辨率渲染。
- **Shapely + pyclipper**：用于票据版面分析（表格线检测、字段区域定位），将 OCR 识别结果按几何位置归位到对应字段。
- **decimal**：财务金额必须使用定点十进制运算，杜绝 `float` 精度误差导致的借贷不平。

---

## 3. 核心模块设计

### 3.1 模块划分

| 模块 | 功能说明 | 关键依赖 |
|---|---|---|
| 票据识别 | 图像/PDF 导入、预处理、OCR 识别、字段提取、人工校正 | OpenCV, PyMuPDF, RapidOCR, Shapely, pyclipper |
| 凭证管理 | 记账凭证填制、审核、查询、打印；借贷平衡校验 | decimal, SQLite |
| 账簿管理 | 总账、明细账、日记账、科目余额表 | SQLite |
| 报表管理 | 资产负债表、利润表、现金流量表生成与导出 | SQLite, lxml |
| 基础设置 | 会计科目、往来单位、部门、职员、币种维护 | SQLite |
| 系统管理 | 用户权限、数据备份/恢复、操作日志、结账 | SQLite, pywin32 |

### 3.2 票据 OCR 识别流水线

这是本系统的核心技术模块，处理流程如下：

```
票据输入 (图片 / PDF)
    │
    ▼
[1] 输入归一化
    - 图片：OpenCV 读取，统一色彩空间
    - PDF：PyMuPDF 渲染为 300 DPI 图像
    │
    ▼
[2] 图像预处理 (OpenCV)
    - 去噪（高斯模糊/中值滤波）
    - 倾斜矫正（霍夫变换检测文本行角度）
    - 对比度增强 / 自适应二值化
    - 边缘检测 + 轮廓提取（定位票据边界）
    │
    ▼
[3] 版面分析 (Shapely + pyclipper)
    - 检测表格线（横线/竖线）
    - 划分字段区域（发票代码、号码、日期、金额、税额等）
    - 构建区域几何拓扑（Shapely 多边形）
    │
    ▼
[4] OCR 识别 (RapidOCR + ONNX Runtime)
    - 文本检测（ch_PP-OCRv3_det）：定位所有文字行
    - 方向分类（ch_ppocr_mobile_v2.0_cls）：纠正文字方向
    - 文本识别（ch_PP-OCRv3_rec）：逐行转文字
    - 多进程并行：批量票据时使用 multiprocessing 加速
    │
    ▼
[5] 字段映射与结构化
    - 将 OCR 文本行按几何位置归入对应字段区域
    - 关键字段正则校验（发票号码格式、金额格式、日期格式）
    - 金额大小写一致性校验
    │
    ▼
[6] 人工校正界面 (Tkinter)
    - 左侧原始票据图像，右侧识别结果表格
    - 低置信度字段高亮提示
    - 用户确认/修改后写入凭证
```

### 3.3 财务数据模型（核心表）

| 表名 | 用途 | 关键字段 |
|---|---|---|
| `account_subject` | 会计科目 | 科目编码、科目名称、科目类别、余额方向、父级科目 |
| `voucher` | 记账凭证 | 凭证号、凭证日期、附件数、制单人、审核人、状态 |
| `voucher_entry` | 凭证明细 | 凭证ID、摘要、科目编码、借方金额、贷方金额 |
| `invoice` | 票据/发票 | 票据类型、发票代码、发票号码、开票日期、金额、税额、价税合计、OCR置信度 |
| `general_ledger` | 总账 | 科目编码、年度、期初余额、本期借方、本期贷方、期末余额 |
| `detail_ledger` | 明细账 | 科目编码、日期、凭证号、摘要、借方、贷方、余额 |
| `customer` | 往来单位 | 单位编码、名称、税号、地址电话、开户行账号 |
| `sys_user` | 系统用户 | 用户名、密码哈希、角色 |
| `sys_log` | 操作日志 | 时间、用户、操作类型、操作内容 |

**账务一致性约束**：
- 每张凭证借贷合计必须相等（应用层 + 数据库触发器双重校验）；
- 金额字段使用 `DECIMAL(18,2)` 存储，Python 端用 `decimal.Decimal` 运算；
- 凭证审核后不可修改，需反审核才能编辑；
- 结账后当期凭证冻结。

---

## 4. 数据存储与备份

### 4.1 数据库文件

- 主数据库：`data/finance.db`（SQLite 单文件）
- 票据图像：`data/invoices/` 目录，按年月分子目录存储
- 备份文件：`backup/finance_YYYYMMDD_HHMMSS.db`

### 4.2 备份策略

| 方式 | 触发时机 | 保留策略 |
|---|---|---|
| 自动备份 | 每次结账时 | 保留最近 10 份 |
| 手动备份 | 用户在系统管理中点击 | 不限，用户自行管理 |
| 退出备份 | 程序退出时（可选开关） | 保留最近 5 份 |

---

## 5. 关键技术点

### 5.1 OCR 模型与推理

- **模型**：PaddleOCR 中文模型的 ONNX 转换版
  - 检测模型：`ch_PP-OCRv3_det_infer.onnx`（2.4 MB）
  - 识别模型：`ch_PP-OCRv3_rec_infer.onnx`（10.7 MB）
  - 方向分类模型：`ch_ppocr_mobile_v2.0_cls_infer.onnx`（0.6 MB）
- **推理后端**：ONNX Runtime CPU 版，启用线程池优化
- **配置管理**：PyYAML 管理模型路径、阈值（检测阈值 0.3、识别置信度阈值 0.5）
- **性能**：单张 A4 票据识别耗时约 1–3 秒（CPU），批量处理启用多进程

### 5.2 图像处理流水线

1. **去噪**：高斯模糊（核 3×3）去除扫描噪声
2. **倾斜矫正**：霍夫变换检测文本行角度，旋转矫正（±15° 范围内）
3. **二值化**：自适应高斯阈值，应对光照不均
4. **边缘增强**：Canny 边缘检测 + 形态学操作，定位票据外框
5. **透视变换**：根据四角点做透视矫正，将梯形票据转正为矩形

### 5.3 高精度财务计算

- 所有金额运算使用 `decimal.Decimal`，精度设为 28 位
- 禁止使用 `float` 进行金额加减乘除
- 税率计算：`税额 = 价税合计 / (1 + 税率) * 税率`，使用 `Decimal` 精确运算后四舍五入到分
- 借贷平衡校验：`sum(借方) == sum(贷方)`，使用 `Decimal` 比较

### 5.4 打包与部署

- **打包工具**：PyInstaller（onefile 模式）
- **入口脚本**：`finance_app.py`
- **运行时钩子**：
  - `pyi_rth__tkinter`：Tkinter 资源路径初始化
  - `pyi_rth_multiprocessing`：多进程 freeze 支持
  - `pyi_rth_pkgutil`：包资源访问
  - `pyi_rth_inspect`：模块自省
- **数据文件**：OCR 模型（.onnx）、Tcl/Tk 资源、配置文件全部打入包内
- **首次启动**：onefile 模式自动解压到 `%TEMP%\_MEIxxxxxx` 目录，退出后清理

---

## 6. 安全与性能

### 6.1 数据安全

| 措施 | 说明 |
|---|---|
| 离线运行 | OCR 推理、数据存储均在本地，无网络上传 |
| 密码存储 | 用户密码使用 SHA-256 + 盐值哈希，不明文存储 |
| 操作日志 | 所有增删改操作记录日志，可追溯 |
| 数据备份 | 多级备份策略，防止数据丢失 |
| 权限控制 | 制单/审核/记账/结账权限分离 |

### 6.2 性能指标

| 指标 | 目标值 |
|---|---|
| 程序启动时间 | ≤ 5 秒（含运行时解压） |
| 单张票据 OCR 识别 | ≤ 3 秒（CPU） |
| 凭证保存响应 | ≤ 0.5 秒 |
| 报表生成（万级凭证） | ≤ 3 秒 |
| 内存占用（空闲） | ≤ 200 MB |
| 内存占用（OCR 峰值） | ≤ 2 GB |

---

## 7. 测试策略

### 7.1 测试分层

| 测试类型 | 覆盖范围 | 工具/方法 |
|---|---|---|
| 单元测试 | 金额计算、日期校验、凭证借贷平衡、科目编码规则 | pytest |
| 集成测试 | 数据库 CRUD、凭证录入→审核→记账全流程、备份恢复 | pytest + SQLite 内存库 |
| OCR 准确率测试 | 标准票据样本集识别率、字段提取准确率 | 样本集回归测试 |
| UI 测试 | 主窗口启动、菜单导航、对话框交互 | 手动 + 自动化冒烟 |
| 性能测试 | 大数据量下报表生成速度、批量 OCR 耗时 | 计时脚本 |

### 7.2 关键测试用例

1. **借贷平衡**：借方合计 ≠ 贷方合计时，凭证保存必须被拒绝并提示；
2. **金额精度**：`0.1 + 0.2` 类运算必须等于 `0.3`（Decimal 验证）；
3. **发票校验**：发票号码格式、日期合法性、金额大小写一致性；
4. **结账完整性**：结账后当期凭证不可修改，反结账后恢复可编辑；
5. **备份恢复**：备份文件可完整恢复，恢复后数据与备份前一致；
6. **OCR 回归**：标准样本集识别准确率不低于 95%，关键字段（金额、日期）不低于 98%。

---

## 8. 后续迭代方向

| 优先级 | 方向 | 说明 |
|---|---|---|
| P0 | 联网版/多用户 | 从 SQLite 迁移至 PostgreSQL/MySQL，支持多终端协同 |
| P1 | 电子发票对接 | 对接增值税发票综合服务平台，自动获取电子发票 |
| P1 | 银企直连 | 对接银行 API，自动获取银行流水、智能对账 |
| P2 | 财务分析看板 | 基于历史数据的趋势分析、比率分析、预警提示 |
| P2 | 移动端 | 手机端查询/审批，数据同步至云端 |
| P3 | AI 智能凭证 | 基于历史凭证的自动凭证生成建议 |

---

## 附录 A：打包文件构成（CArchive 目录摘要）

| 类别 | 数量 | 说明 |
|---|---|---|
| 二进制/DLL/PYD | 1072 | Python 运行时、第三方库编译扩展、Tcl/Tk 资源 |
| 主脚本 | 6 | finance_app（主程序）+ 5 个 PyInstaller 运行时钩子 |
| PYZ 归档 | 1 | PYZ.pyz（7.3 MB，纯 Python 模块集合） |
| 配置/选项 | 1 | pyi-contents-directory _internal |

**主要第三方库体积占比**：
- OpenCV (cv2.pyd + ffmpeg)：约 117 MB
- ONNX Runtime：约 37 MB
- PyMuPDF (mupdf)：约 39 MB
- NumPy + OpenBLAS：约 25 MB
- RapidOCR 模型：约 14 MB
- Tcl/Tk 运行时：约 5 MB

---

*本文档基于对 `财务管理系统.exe` 的 PyInstaller CArchive 静态分析编制，技术栈与模块设计均有打包目录中的实际文件作为依据。*
